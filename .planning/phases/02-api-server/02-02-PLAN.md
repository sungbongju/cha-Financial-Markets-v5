---
phase: 02-api-server
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - server/routes/db-save.js
  - server/index.js
  - server/scripts/generate-cache.js
  - server/scripts/cache-products.json
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]

must_haves:
  truths:
    - "대화 기록이 학교서버 DB에 저장된다 (aiforalab.com/finmarket-api/api.php)"
    - "같은 질문에 대한 TTS가 이미 생성된 경우 서버 캐시에서 즉시 반환된다"
    - "배치 스크립트 실행 시 38개 기본 상품 질문에 대한 TTS mp3 파일이 생성된다"
    - "DB 저장 실패가 SSE 응답을 차단하지 않는다 (비동기, fire-and-forget)"
  artifacts:
    - path: "server/routes/db-save.js"
      provides: "학교서버 DB 비동기 POST 저장 라우트"
      exports: ["router (POST /api/db-save)"]
    - path: "server/scripts/generate-cache.js"
      provides: "38개 기본 상품 TTS 사전 생성 배치 스크립트"
      contains: "PRODUCT_QUESTIONS"
    - path: "server/scripts/cache-products.json"
      provides: "캐시 생성 대상 상품명 + 기본 질문 목록"
  key_links:
    - from: "server/routes/sts-stream.js"
      to: "server/routes/db-save.js"
      via: "fire-and-forget fetch POST (비동기, await 없음)"
      pattern: "fetch.*db-save"
    - from: "server/routes/humelo-tts-stream.js"
      to: "server/tts-cache/"
      via: "캐시 키 조회 → 파일 존재 시 파일 스트림 반환"
      pattern: "tts-cache"
---

<objective>
대화 기록 DB 저장(비동기)과 TTS 캐시 시스템을 구축하여 반복 질문의 레이턴시를 0으로 만든다.

Purpose: DATA-01은 학습 데이터 수집, DATA-02/03은 자주 묻는 상품 질문의 TTS를 미리 생성하여 즉시 응답.
Output: db-save 라우트, TTS 파일 캐시 레이어, 배치 생성 스크립트
</objective>

<execution_context>
@C:/Users/sungs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sungs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@server/index.js
@server/routes/sts-stream.js
@server/routes/humelo-tts-stream.js

@.planning/phases/02-api-server/02-01-SUMMARY.md

<interfaces>
<!-- 학교서버 API 스펙 (기존 v2/v3에서 검증된 패턴) -->
```
POST https://aiforalab.com/finmarket-api/api.php
Content-Type: application/json
Body: {
  action: 'save_chat',
  session_id: 'v5_xxxxxxxx',   // v5_ 접두사
  user_message: string,
  assistant_reply: string,
  mode: 'sts'
}
응답: { success: true } 또는 오류
```

<!-- 02-01 결과 라우트 구조 (TTS 캐시 레이어가 연결되는 지점) -->
```javascript
// server/routes/humelo-tts-stream.js (POST /api/humelo-tts-stream)
// 입력: { text, voiceName?, speed?, emotion? }
// 출력: audio/mpeg 바이너리 스트림

// TTS 캐시 키: MD5(text.trim().toLowerCase()) → server/tts-cache/{hash}.mp3
// 캐시 히트 시: fs.createReadStream(cachePath).pipe(res) 로 즉시 전달
// 캐시 미스 시: Humelo API 호출 후 파일 저장 + 클라이언트 전달 동시 진행
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DB 비동기 저장 라우트 + TTS 파일 캐시 레이어</name>
  <files>server/routes/db-save.js, server/routes/humelo-tts-stream.js, server/index.js</files>
  <action>
**A. server/routes/db-save.js 작성 (DATA-01)**

```javascript
'use strict';
const express = require('express');
const router = express.Router();

const DB_API_URL = 'https://aiforalab.com/finmarket-api/api.php';

// POST /api/db-save — fire-and-forget, 항상 200 즉시 반환
router.post('/db-save', (req, res) => {
  const { session_id, user_message, assistant_reply, mode = 'sts' } = req.body || {};

  // 클라이언트에는 즉시 200 반환 (비동기 처리)
  res.json({ queued: true });

  // 백그라운드에서 학교서버에 저장 (실패해도 클라이언트 무관)
  if (session_id && user_message && assistant_reply) {
    fetch(DB_API_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ action: 'save_chat', session_id, user_message, assistant_reply, mode })
    })
    .then(r => { if (!r.ok) console.warn('[db-save] HTTP', r.status); })
    .catch(e => console.warn('[db-save] error:', e.message));
  }
});

module.exports = router;
```

`server/index.js`에 `app.use('/api', require('./routes/db-save'));` 추가.

---

**B. TTS 파일 캐시 레이어 구축 (DATA-02)**

`server/routes/humelo-tts-stream.js`에 파일 캐시 레이어를 추가한다.

필요 모듈: `require('crypto')` (내장), `require('fs')` (내장), `require('path')` (내장)

캐시 디렉토리: `path.join(__dirname, '../tts-cache')` — `fs.mkdirSync(dir, { recursive: true })` 라우트 파일 상단에서 초기화.

```javascript
const CACHE_DIR = path.join(__dirname, '../tts-cache');
fs.mkdirSync(CACHE_DIR, { recursive: true });

function getCacheKey(text) {
  return require('crypto').createHash('md5').update(text.trim().toLowerCase()).digest('hex');
}
```

**라우트 로직 수정 (`router.post('/humelo-tts-stream', ...)`):**

1. 캐시 키 계산: `const cacheKey = getCacheKey(text);`
2. 캐시 파일 경로: `const cachePath = path.join(CACHE_DIR, cacheKey + '.mp3');`
3. **캐시 히트:** `if (fs.existsSync(cachePath))` → 응답 헤더 설정(Content-Type: audio/mpeg, X-Cache: HIT) + `fs.createReadStream(cachePath).pipe(res)` 즉시 반환
4. **캐시 미스:**
   - Humelo API 호출
   - 성공 시: `reader`에서 읽은 청크를 `res.write()` + 캐시 파일에도 동시 기록 (`fs.createWriteStream(cachePath, { flags: 'w' })`)
   - 캐시 저장 실패 → 로그만, 응답은 계속
   - 응답 헤더: `X-Cache: MISS`

**캐시 파일 동시 기록 패턴:**
```javascript
const cacheStream = fs.createWriteStream(cachePath + '.tmp');
let cacheError = false;
cacheStream.on('error', (e) => { cacheError = true; console.warn('[tts-cache] write error:', e.message); });

// 청크 루프
while (true) {
  const { done, value } = await reader.read();
  if (done) break;
  res.write(Buffer.from(value));
  if (!cacheError) cacheStream.write(Buffer.from(value));
}

cacheStream.end();
// tmp → 실제 파일로 rename (불완전 파일 방지)
if (!cacheError) {
  fs.renameSync(cachePath + '.tmp', cachePath);
  console.log('[tts-cache] saved:', cacheKey);
}
res.end();
```
  </action>
  <verify>
    <automated>
      cd /c/dev/AI-BIZ-Page/cha-Financial-Markets-v5/server && node -e "const r = require('./routes/db-save'); console.log('db-save router:', typeof r.handle || typeof r)"
    </automated>
  </verify>
  <done>
    - `server/routes/db-save.js` 존재, POST /api/db-save 라우트 정의
    - `server/routes/humelo-tts-stream.js`에 `tts-cache` 디렉토리 초기화 + getCacheKey 함수 존재
    - `server/routes/humelo-tts-stream.js`에 `X-Cache: HIT` 및 `fs.createReadStream` 존재
    - `server/index.js`에 `/api/db-save` 라우트 마운트 존재
    - `node -e "require('./routes/db-save')"` 오류 없음
    - `node -e "require('./routes/humelo-tts-stream')"` 오류 없음
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TTS 사전 생성 배치 스크립트</name>
  <files>server/scripts/generate-cache.js, server/scripts/cache-products.json</files>
  <action>
**A. server/scripts/cache-products.json 작성 (DATA-03)**

38개 핵심 상품에 대한 기본 질문 목록을 정의한다. 각 상품당 1개 질문:

```json
[
  { "product": "정기예금", "question": "정기예금이 뭔가요?" },
  { "product": "정기적금", "question": "정기적금이 뭔가요?" },
  { "product": "주택청약저축", "question": "주택청약저축이 뭔가요?" },
  { "product": "주식", "question": "주식이 뭔가요?" },
  { "product": "우선주", "question": "우선주가 뭔가요?" },
  { "product": "국고채", "question": "국고채가 뭔가요?" },
  { "product": "회사채", "question": "회사채가 뭔가요?" },
  { "product": "CD", "question": "씨디가 뭔가요?" },
  { "product": "RP", "question": "알피가 뭔가요?" },
  { "product": "전환사채", "question": "전환사채가 뭔가요?" },
  { "product": "ABS", "question": "에이비에스가 뭔가요?" },
  { "product": "ETF", "question": "이티에프가 뭔가요?" },
  { "product": "MMF", "question": "엠엠에프가 뭔가요?" },
  { "product": "REITs", "question": "리츠가 뭔가요?" },
  { "product": "ELS", "question": "이엘에스가 뭔가요?" },
  { "product": "ETN", "question": "이티엔이 뭔가요?" },
  { "product": "ELD", "question": "이엘디가 뭔가요?" },
  { "product": "ELW", "question": "이엘더블유가 뭔가요?" },
  { "product": "주가지수선물", "question": "주가지수선물이 뭔가요?" },
  { "product": "주가지수옵션", "question": "주가지수옵션이 뭔가요?" },
  { "product": "금", "question": "금 투자가 뭔가요?" },
  { "product": "달러", "question": "달러 투자가 뭔가요?" },
  { "product": "금전신탁", "question": "금전신탁이 뭔가요?" },
  { "product": "MMT", "question": "엠엠티가 뭔가요?" },
  { "product": "CMA", "question": "씨엠에이가 뭔가요?" },
  { "product": "연금저축", "question": "연금저축이 뭔가요?" },
  { "product": "IRP", "question": "아이알피가 뭔가요?" },
  { "product": "ISA", "question": "아이에스에이가 뭔가요?" },
  { "product": "생명보험", "question": "생명보험이 뭔가요?" },
  { "product": "손해보험", "question": "손해보험이 뭔가요?" },
  { "product": "신용대출", "question": "신용대출이 뭔가요?" },
  { "product": "담보대출", "question": "담보대출이 뭔가요?" },
  { "product": "신용카드", "question": "신용카드가 뭔가요?" },
  { "product": "콜", "question": "콜머니가 뭔가요?" },
  { "product": "기업어음", "question": "기업어음이 뭔가요?" },
  { "product": "MMDA", "question": "엠엠디에이가 뭔가요?" },
  { "product": "MBS", "question": "엠비에스가 뭔가요?" },
  { "product": "CDO", "question": "씨디오가 뭔가요?" }
]
```

**B. server/scripts/generate-cache.js 작성 (DATA-03)**

Node.js 스크립트. `node scripts/generate-cache.js` 로 실행.

동작:
1. `cache-products.json` 로드
2. 각 상품 질문에 대해 순서대로 (병렬 아님 — Humelo API 레이트 리밋 방지):
   a. `POST http://127.0.0.1:9000/api/sts-stream` 에 `{message: question}` 으로 SSE 스트리밍 요청
   b. SSE의 `text` 이벤트에서 문장 수집 → 전체 텍스트 조합
   c. `POST http://127.0.0.1:9000/api/humelo-tts-stream` 에 `{text: fullText}` 요청
   d. 응답 바이너리를 `server/tts-cache/{md5(fullText)}.mp3`에 저장
   e. `X-Cache: HIT` 헤더가 있으면 이미 캐시됨 → 스킵
3. 진행 상황 콘솔 출력: `[N/38] 정기예금 → CACHED` 또는 `GENERATED (1234 bytes)`
4. 오류 시 로그 후 다음 상품으로 계속

```javascript
'use strict';
require('dotenv').config({ path: require('path').join(__dirname, '../.env') });
const fs = require('fs');
const path = require('path');
const crypto = require('crypto');

const BASE_URL = 'http://127.0.0.1:9000';
const CACHE_DIR = path.join(__dirname, '../tts-cache');
const PRODUCTS = require('./cache-products.json');

function getCacheKey(text) {
  return crypto.createHash('md5').update(text.trim().toLowerCase()).digest('hex');
}

// SSE 파싱 헬퍼 — Node.js fetch로 SSE 읽기
async function fetchSTSText(question) {
  const res = await fetch(`${BASE_URL}/api/sts-stream`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ message: question, history: [] })
  });
  if (!res.ok) throw new Error(`STS HTTP ${res.status}`);

  const reader = res.body.getReader();
  const dec = new TextDecoder();
  let buf = '', sentences = [];

  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    buf += dec.decode(value, { stream: true });
    const lines = buf.split('\n');
    buf = lines.pop() || '';
    for (const line of lines) {
      if (!line.startsWith('data: ')) continue;
      try {
        const d = JSON.parse(line.slice(6));
        if (d.sentence) sentences.push(d.sentence);
      } catch {}
    }
  }
  return sentences.join(' ');
}

async function main() {
  fs.mkdirSync(CACHE_DIR, { recursive: true });
  let generated = 0, skipped = 0, errors = 0;

  for (let i = 0; i < PRODUCTS.length; i++) {
    const { product, question } = PRODUCTS[i];
    const prefix = `[${i+1}/${PRODUCTS.length}] ${product}`;
    try {
      // 1. STS 텍스트 생성
      const text = await fetchSTSText(question);
      if (!text) { console.log(`${prefix} → EMPTY (skip)`); errors++; continue; }

      // 2. 캐시 파일 존재 여부 확인
      const cacheKey = getCacheKey(text);
      const cachePath = path.join(CACHE_DIR, cacheKey + '.mp3');
      if (fs.existsSync(cachePath)) {
        console.log(`${prefix} → CACHED (${cacheKey.slice(0, 8)})`);
        skipped++;
        continue;
      }

      // 3. TTS 생성
      const ttsRes = await fetch(`${BASE_URL}/api/humelo-tts-stream`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text })
      });
      if (!ttsRes.ok) throw new Error(`TTS HTTP ${ttsRes.status}`);

      const buffer = Buffer.from(await ttsRes.arrayBuffer());
      if (buffer.length < 100) throw new Error('TTS response too small');

      fs.writeFileSync(cachePath, buffer);
      console.log(`${prefix} → GENERATED (${buffer.length} bytes, ${cacheKey.slice(0, 8)})`);
      generated++;

      // 레이트 리밋 방지: 500ms 딜레이
      await new Promise(r => setTimeout(r, 500));

    } catch (e) {
      console.error(`${prefix} → ERROR: ${e.message}`);
      errors++;
    }
  }

  console.log(`\n완료: ${generated}개 생성, ${skipped}개 캐시 재사용, ${errors}개 오류`);
}

main().catch(console.error);
```

`server/package.json` scripts에 추가:
```json
"generate-cache": "node scripts/generate-cache.js"
```
  </action>
  <verify>
    <automated>
      cd /c/dev/AI-BIZ-Page/cha-Financial-Markets-v5/server && node -e "const p = require('./scripts/cache-products.json'); console.log('products:', p.length, 'first:', p[0].product)"
    </automated>
  </verify>
  <done>
    - `server/scripts/cache-products.json` 존재, 38개 항목 (length === 38)
    - `server/scripts/generate-cache.js` 존재
    - `node -e "require('./scripts/cache-products.json')"` 오류 없음
    - `generate-cache.js`에 `getCacheKey`, `fetchSTSText`, `PRODUCTS.length` 존재
    - `server/package.json` scripts에 `"generate-cache"` 항목 존재
  </done>
</task>

</tasks>

<verification>
로컬 검증:
1. `node -e "require('./routes/db-save')"` — 오류 없음
2. `node -e "const p = require('./scripts/cache-products.json'); console.log(p.length)"` — 38 출력
3. `cat server/routes/humelo-tts-stream.js | grep 'X-Cache'` — HIT/MISS 캐시 헤더 확인
4. `cat server/routes/humelo-tts-stream.js | grep 'createReadStream'` — 캐시 히트 스트림 확인
5. `cat server/routes/humelo-tts-stream.js | grep 'renameSync'` — 원자적 캐시 저장 확인

실서버 검증 (SSH 후):
- 배치 스크립트: `cd ~/finbot/server && npm run generate-cache`
- 캐시 히트 확인: `curl -I -X POST http://localhost:9000/api/humelo-tts-stream -H 'Content-Type: application/json' -d '{"text":"정기예금이란..."}'`
  - 헤더에 `X-Cache: MISS` 첫 번째, `X-Cache: HIT` 두 번째 확인
</verification>

<success_criteria>
- DATA-01: POST /api/db-save 가 학교서버에 비동기로 저장하며 클라이언트에 즉시 200 반환
- DATA-02: 동일 텍스트 TTS 요청 시 캐시 파일에서 즉시 스트림 반환 (Humelo API 호출 없음)
- DATA-03: `npm run generate-cache` 실행 시 38개 상품 기본 답변 TTS mp3 생성 완료
</success_criteria>

<output>
완료 후 `.planning/phases/02-api-server/02-02-SUMMARY.md` 작성
</output>
