---
phase: 03-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy-server.py
autonomous: true
requirements:
  - FE-01

must_haves:
  truths:
    - "Middleton 서버에 최신 server/ 코드가 배포되어 있다"
    - "PM2 finbot-server 프로세스가 online 상태다"
    - "curl http://localhost:9000/health가 200 OK를 반환한다"
    - ".env 파일에 HUMELO_API_KEY가 설정되어 있다"
  artifacts:
    - path: "scripts/deploy-server.py"
      provides: "paramiko SSH 배포 스크립트"
    - path: "server/.env.example"
      provides: "환경변수 템플릿 (HUMELO_API_KEY)"
  key_links:
    - from: "scripts/deploy-server.py"
      to: "1.223.219.123:7822 (student04)"
      via: "paramiko SSH + SFTP"
      pattern: "paramiko\\.SSHClient"
---

<objective>
server/ 코드를 paramiko SSH로 Middleton에 배포하고 PM2를 재시작한다.

Purpose: Phase 2에서 완성된 API 서버 코드를 실제 서버에 올려 프론트엔드가 연결할 수 있게 한다.
Output: Middleton 포트 9000에서 finbot-server가 최신 코드로 실행 중
</objective>

<execution_context>
@C:/Users/sungs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sungs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-api-server/02-01-SUMMARY.md
@.planning/phases/02-api-server/02-02-SUMMARY.md

서버 접속 정보:
- SSH: 1.223.219.123:7822, student04, chacha2025
- 서버 경로: /home/student04/finbot/server/
- PM2 프로세스명: finbot-server
- 포트: 9000 (127.0.0.1 바인딩)

배포할 파일 (server/ 디렉토리):
- index.js
- package.json
- routes/sts-stream.js
- routes/humelo-tts-stream.js
- routes/humelo-tts.js
- routes/db-save.js
- scripts/generate-cache.js
- scripts/cache-products.json
- .env.example

주의: node_modules/, tts-cache/ 는 전송 제외
주의: .env 파일은 서버에 이미 있거나 수동 생성 필요 (HUMELO_API_KEY 포함)

Phase 1에서 확인된 사항:
- Node.js v24.14.1 설치됨 (nvm)
- PM2는 npx pm2로 실행
- crontab @reboot으로 PM2 자동 시작 설정됨
- 서버 경로 /home/student04/finbot/ 이미 존재

<interfaces>
<!-- paramiko 사용 패턴 (Windows 환경) -->
import paramiko
import os

ssh = paramiko.SSHClient()
ssh.set_missing_host_key_policy(paramiko.AutoAddPolicy())
ssh.connect('1.223.219.123', port=7822, username='student04', password='chacha2025')

sftp = ssh.open_sftp()
# 파일 전송: sftp.put(local_path, remote_path)
# 디렉토리 생성: stdin, stdout, stderr = ssh.exec_command('mkdir -p /home/student04/finbot/server/routes')

# npm install:
stdin, stdout, stderr = ssh.exec_command('cd /home/student04/finbot/server && npm install --production')
# PM2 재시작:
stdin, stdout, stderr = ssh.exec_command('cd /home/student04/finbot && npx pm2 restart finbot-server 2>&1 || npx pm2 start server/index.js --name finbot-server')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: paramiko 배포 스크립트 작성 및 실행</name>
  <files>scripts/deploy-server.py</files>
  <action>
1. `scripts/` 디렉토리 생성 후 `scripts/deploy-server.py` 작성:

```python
#!/usr/bin/env python3
"""
Middleton 서버에 finbot server/ 코드를 배포하는 스크립트.
paramiko SSH + SFTP 사용 (Windows 환경, sshpass 불가)
"""
import paramiko
import os
import sys

SSH_HOST = '1.223.219.123'
SSH_PORT = 7822
SSH_USER = 'student04'
SSH_PASS = 'chacha2025'
REMOTE_BASE = '/home/student04/finbot/server'

# 전송할 파일 목록 (로컬 server/ 기준 상대경로)
FILES = [
    'index.js',
    'package.json',
    'routes/sts-stream.js',
    'routes/humelo-tts-stream.js',
    'routes/humelo-tts.js',
    'routes/db-save.js',
    'scripts/generate-cache.js',
    'scripts/cache-products.json',
    '.env.example',
]

def run_cmd(ssh, cmd, check=True):
    stdin, stdout, stderr = ssh.exec_command(cmd)
    out = stdout.read().decode().strip()
    err = stderr.read().decode().strip()
    if out:
        print(f'  OUT: {out}')
    if err:
        print(f'  ERR: {err}')
    return out, err

def main():
    local_base = os.path.join(os.path.dirname(__file__), '..', 'server')
    local_base = os.path.normpath(local_base)

    print(f'[deploy] Connecting to {SSH_HOST}:{SSH_PORT}...')
    ssh = paramiko.SSHClient()
    ssh.set_missing_host_key_policy(paramiko.AutoAddPolicy())
    ssh.connect(SSH_HOST, port=SSH_PORT, username=SSH_USER, password=SSH_PASS)
    print('[deploy] Connected.')

    sftp = ssh.open_sftp()

    # 원격 디렉토리 생성
    for d in ['', 'routes', 'scripts', 'tts-cache']:
        run_cmd(ssh, f'mkdir -p {REMOTE_BASE}/{d}')
    print('[deploy] Remote directories ready.')

    # 파일 전송
    for rel_path in FILES:
        local_path = os.path.join(local_base, rel_path.replace('/', os.sep))
        remote_path = f'{REMOTE_BASE}/{rel_path}'
        if not os.path.exists(local_path):
            print(f'  [skip] {rel_path} (not found locally)')
            continue
        sftp.put(local_path, remote_path)
        print(f'  [ok] {rel_path}')

    sftp.close()
    print('[deploy] Files transferred.')

    # npm install (production only)
    print('[deploy] Running npm install...')
    run_cmd(ssh, f'cd {REMOTE_BASE} && npm install --production 2>&1')

    # .env 파일 존재 확인
    out, _ = run_cmd(ssh, f'test -f {REMOTE_BASE}/.env && echo EXISTS || echo MISSING')
    if 'MISSING' in out:
        print('[deploy] WARNING: .env file missing on server!')
        print(f'[deploy]   Create it: echo "HUMELO_API_KEY=<your-key>" > {REMOTE_BASE}/.env')
        print('[deploy]   Then run: cd ~/finbot && npx pm2 restart finbot-server')
    else:
        print('[deploy] .env file found.')
        # PM2 재시작
        print('[deploy] Restarting PM2...')
        run_cmd(ssh, f'cd /home/student04/finbot && npx pm2 restart finbot-server 2>&1 || npx pm2 start server/index.js --name finbot-server 2>&1')

    # 헬스체크
    print('[deploy] Health check...')
    out, err = run_cmd(ssh, 'curl -s http://localhost:9000/health')
    if '"status":"ok"' in out:
        print('[deploy] SUCCESS: Server is healthy.')
    else:
        print('[deploy] WARNING: Health check failed. Check PM2 logs:')
        print('[deploy]   npx pm2 logs finbot-server --lines 20')

    ssh.close()
    print('[deploy] Done.')

if __name__ == '__main__':
    main()
```

2. 스크립트 실행:
   ```bash
   cd /c/dev/AI-BIZ-Page/cha-Financial-Markets-v5
   python scripts/deploy-server.py
   ```

3. `.env` MISSING 경고 출력 시:
   - 별도 SSH 세션에서 `.env` 파일 수동 생성 후 PM2 재시작
   - 또는 스크립트 내에서 HUMELO_API_KEY를 환경변수로 전달하는 추가 처리

주의: node_modules/ 전송 제외 (npm install로 원격 설치). tts-cache/ 디렉토리만 생성.
  </action>
  <verify>
    <automated>python scripts/deploy-server.py 2>&1 | tail -5</automated>
  </verify>
  <done>
- scripts/deploy-server.py 파일 존재
- 스크립트 실행 후 "[deploy] SUCCESS: Server is healthy." 출력
- 또는 .env MISSING 경고 출력 (이 경우 .env 생성 후 PM2 재시작 필요)
  </done>
</task>

</tasks>

<verification>
- `python scripts/deploy-server.py` → SUCCESS 메시지 확인
- SSH 직접 접속 후: `curl http://localhost:9000/health` → `{"status":"ok",...}`
- `npx pm2 list` → finbot-server online 상태
</verification>

<success_criteria>
- Middleton 포트 9000에서 finbot-server가 최신 코드(Phase 2 완성본)로 실행 중
- `curl http://localhost:9000/health` 200 OK
- PM2 프로세스 online 상태
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-02-SUMMARY.md`
</output>
