---
phase: 03-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
  - public/js/auth.js
  - vercel.json
autonomous: false
requirements:
  - FE-01
  - FE-03

must_haves:
  truths:
    - "cha-financial-markets-v5.vercel.app에서 STS 모드 버튼이 표시된다"
    - "STS 질문 시 fetch가 middleton.p-e.kr/finbot/api/sts-stream으로 향한다"
    - "채팅 버블에 사용자 질문과 AI 응답 텍스트가 표시된다"
    - "음성 상태 레이블(듣는 중/생각 중/답변 중)이 STS 진행 중 시각적으로 바뀐다"
    - "카카오 로그인이 정상 동작한다 (finmarket_db 연동)"
  artifacts:
    - path: "public/index.html"
      provides: "STS 모드 UI (채팅 버블 + 음성 상태 + API URL = Middleton)"
      min_lines: 1000
    - path: "public/js/auth.js"
      provides: "카카오 로그인 + finmarket_db 연동"
    - path: "vercel.json"
      provides: "Vercel 정적 파일 서빙 설정 (public/ → /)"
  key_links:
    - from: "public/index.html"
      to: "https://middleton.p-e.kr/finbot/api/sts-stream"
      via: "fetch POST in STS handler"
      pattern: "middleton\\.p-e\\.kr/finbot/api/sts-stream"
    - from: "public/index.html"
      to: "https://middleton.p-e.kr/finbot/api/humelo-tts-stream"
      via: "AudioQueue streamUrl construction"
      pattern: "middleton\\.p-e\\.kr/finbot/api/humelo-tts-stream"
---

<objective>
v3 프론트엔드를 v5에 복사하고 API URL을 Middleton nginx 프록시로 교체하여 Vercel에 배포한다.

Purpose: STS 음성 대화가 Middleton 서버를 직접 바라보도록 연결하여 레이턴시를 최소화한다.
Output: cha-financial-markets-v5.vercel.app에서 STS 모드가 동작하는 Vercel 배포
</objective>

<execution_context>
@C:/Users/sungs/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/sungs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

소스: ../cha-Financial-Markets-v3/public/index.html (2880줄)
소스: ../cha-Financial-Markets-v3/public/js/auth.js

Phase 2에서 만든 서버 API 엔드포인트:
- POST https://middleton.p-e.kr/finbot/api/sts-stream  — SSE 스트리밍 (엑사원 + 문장 분할)
- GET  https://middleton.p-e.kr/finbot/api/humelo-tts-stream?text=...&voiceName=시아&speed=1.05
- POST https://middleton.p-e.kr/finbot/api/humelo-tts  — Standard TTS 폴백
- POST https://middleton.p-e.kr/finbot/api/db-save     — 대화 DB 저장

v3에서 변경이 필요한 URL (grep 결과):
- 줄 2127: fetch('/api/sts-stream', ...) → 'https://middleton.p-e.kr/finbot/api/sts-stream'
- 줄 2056: fetch('/api/humelo-tts', ...) → 'https://middleton.p-e.kr/finbot/api/humelo-tts'
- 줄 2181: streamUrl = '/api/humelo-tts-stream?...' → 'https://middleton.p-e.kr/finbot/api/humelo-tts-stream?...'
- 줄 2230: fetch('/api/humelo-tts', ...) → (동일, humelo-tts 폴백)
- 줄 2266: fetch('/api/openai-chat', ...) → TTT/FTF 모드 (v5는 STS 전용, 건드리지 않음)
- 줄 1559: SAVE_CHAT_API = 'https://aiforalab.com/finmarket-api/api.php' → 유지 (학교서버 직접)

v3 vercel.json:
```json
{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/public/$1" }
  ]
}
```
v5는 /api/ Vercel Functions 없음 → vercel.json을 정적 서빙으로 단순화 필요

<interfaces>
<!-- v3 index.html STS 관련 핵심 변수 -->
const SAVE_CHAT_API = 'https://aiforalab.com/finmarket-api/api.php';  // 줄 1559 — 유지
// API URL 상수 정의 위치: 줄 1559 근처 (또는 각 fetch 호출부에 하드코딩)

// STS 주요 fetch 호출 위치:
// 줄 2127: fetch('/api/sts-stream', { method:'POST', ... })
// 줄 2181: streamUrl = '/api/humelo-tts-stream?text=' + encodeURIComponent(cleanSentence) + '&voiceName=시아&speed=1.05'
// 줄 2056, 2230: fetch('/api/humelo-tts', { ... })
// 줄 2266: fetch('/api/openai-chat', { ... })  ← TTT 모드 (v5는 미사용 가능)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: v3 프론트엔드 복사 + Middleton API URL 교체 + vercel.json</name>
  <files>public/index.html, public/js/auth.js, vercel.json</files>
  <action>
1. `mkdir -p public/js` 생성 후 v3 파일 복사:
   - `../cha-Financial-Markets-v3/public/index.html` → `public/index.html`
   - `../cha-Financial-Markets-v3/public/js/auth.js` → `public/js/auth.js`

2. `public/index.html` 상단(줄 1559 근처, SAVE_CHAT_API 선언 바로 위)에 API base URL 상수 추가:
   ```javascript
   const MIDDLETON_API = 'https://middleton.p-e.kr/finbot';
   ```

3. STS 관련 fetch URL 교체 (아래 4곳):
   - `fetch('/api/sts-stream',` → `fetch(MIDDLETON_API + '/api/sts-stream',`
   - `'/api/humelo-tts-stream?text='` → `` `${MIDDLETON_API}/api/humelo-tts-stream?text=` ``
   - `fetch('/api/humelo-tts',` (2곳 모두) → `fetch(MIDDLETON_API + '/api/humelo-tts',`

4. db-save URL 처리:
   - v3에 `SAVE_CHAT_API = 'https://aiforalab.com/finmarket-api/api.php'` 그대로 유지
   - Middleton db-save 라우트는 미사용 (학교서버 직접 호출 유지)

5. TTT/FTF 모드의 `/api/openai-chat`, `/api/heygen-*` URL:
   - v5는 STS 전용이므로 해당 fetch URL은 건드리지 않음 (코드 삭제 시 런타임 에러 위험)

6. `vercel.json` 작성 — v5는 Vercel Functions 없음, 정적 서빙만:
   ```json
   {
     "outputDirectory": "public",
     "rewrites": [
       { "source": "/(.*)", "destination": "/$1" }
     ]
   }
   ```
   주의: v3의 `/api/(.*)` rewrite 제거 (v5에 api/ 디렉토리 없음)

7. `public/index.html` 제목은 "금융상품 가이드 — 차의과학대학교 경영학전공" 그대로 유지
  </action>
  <verify>
    <automated>grep -n "middleton.p-e.kr" public/index.html | head -10</automated>
  </verify>
  <done>
- middleton.p-e.kr/finbot/api/sts-stream이 index.html에 최소 1회 이상 등장
- middleton.p-e.kr/finbot/api/humelo-tts-stream이 등장
- vercel.json에 outputDirectory: "public" 설정
- '/api/sts-stream' 문자열(상대경로)이 index.html에 남아있지 않음
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Vercel 배포 후 STS E2E 동작 확인</name>
  <action>
GitHub push 후 Vercel 자동 배포가 완료되면 브라우저에서 STS 엔드-투-엔드 동작을 확인한다.

배포 명령:
```bash
cd /c/dev/AI-BIZ-Page/cha-Financial-Markets-v5
git add public/ vercel.json
git commit -m "feat(frontend): v3 프론트엔드 복사 + Middleton API URL 연결"
git push origin main
```

Vercel 프로젝트: sungbongju/cha-Financial-Markets-v5 (환경변수 없음)
  </action>
  <verify>
    <automated>MISSING — 브라우저 확인 필요 (STS 음성 대화 E2E)</automated>
  </verify>
  <done>사용자가 "approved" 입력 또는 문제 증상 설명</done>
  <what-built>
    v3 프론트엔드 복사 완료, Middleton API URL 교체, vercel.json 설정.
    GitHub push 및 Vercel 자동 배포 후 STS 동작을 확인합니다.
  </what-built>
  <how-to-verify>
    1. https://cha-financial-markets-v5.vercel.app 접속하여 페이지 로드 확인
    2. 브라우저 개발자도구 Network 탭 열기
    3. STS 모드 버튼 클릭 → 마이크 권한 허용
    4. "예금이 뭐예요?"라고 말하거나 텍스트 입력
    5. Network 탭에서 요청 URL이 middleton.p-e.kr/finbot/api/sts-stream인지 확인
    6. 채팅 버블에 응답 텍스트 표시 확인
    7. 음성 상태 레이블 변화 확인 (듣는 중 → 생각 중 → 답변 중)

    nginx 미설정 시 대체 테스트:
    - SSH 터널: `ssh -p 7822 -L 9000:localhost:9000 student04@1.223.219.123`
    - index.html에서 MIDDLETON_API를 임시로 'http://localhost:9000'으로 변경하여 로컬 테스트
  </how-to-verify>
  <resume-signal>정상 동작 확인 시 "approved" 입력. 문제 발생 시 증상 설명</resume-signal>
</task>

</tasks>

<verification>
- `grep -c "middleton.p-e.kr" public/index.html` → 3 이상
- `grep "'/api/sts-stream'" public/index.html` → 결과 없음 (상대경로 제거됨)
- `cat vercel.json | grep outputDirectory` → "public" 확인
- Vercel 배포 후 https://cha-financial-markets-v5.vercel.app 200 OK
</verification>

<success_criteria>
- Vercel 배포 성공 (빌드 오류 없음)
- STS 요청이 middleton.p-e.kr/finbot으로 라우팅됨
- 채팅 버블 + 음성 상태 UI가 정상 동작
- 카카오 로그인 정상 동작
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-01-SUMMARY.md`
</output>
